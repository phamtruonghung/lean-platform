import 'package:flutter_test/flutter_test.dart';
import 'package:lean_platform/main.dart' as entry;

void main() {
  test('main.dart is importable from the VM test runner', () {
    expect(entry.main, isNotNull);
  });
}
